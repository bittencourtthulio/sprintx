---
description: sprintx F4 — gera o ORQUESTRADOR.md, mapa de execução da feature
---

Invoque a skill `sprintx` e execute a F4 ORQUESTRADOR seguindo `references/04-orquestrador.md`.

Feature: $ARGUMENTS (se vazio, use a feature em andamento).

Antes de executar, confirme pela máquina de estados do SKILL.md que a fase atual é a F4. Fora de ordem, recuse: aponte qual fase falta (F1 se não há `base/`, F2 se não há `00-DECISOES.md`, F3 se não há `sprint-01/`) e execute a fase pendente em vez desta.
