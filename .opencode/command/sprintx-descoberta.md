---
description: sprintx F2 — descoberta, entrevista o usuário e registra as decisões
---

Invoque a skill `sprintx` e execute a F2 DESCOBERTA seguindo `references/02-descoberta.md`.

Feature: $ARGUMENTS (se vazio, use a feature em andamento).

Antes de executar, confirme pela máquina de estados do SKILL.md que a fase atual é a F2. Fora de ordem, recuse: se `docs/sprintx/features/<slug>/base/` não existe, diga "Falta a F1 (ingestão)" e execute a F1 em vez desta; se `00-DECISOES.md` já existe sem pendências, diga que a F2 já passou e execute a fase realmente pendente.
